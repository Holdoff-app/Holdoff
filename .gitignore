node_modules/
.env
.DS_Store
generate-icons.js
session-env/

# Android build artifacts
android/.gradle/
android/app/build/
android/build/
android/app/release/
android/app/debug/

# Capacitor dist (CI builds this)
dist/

# Polsia safety (auto-injected)
.env
.env.*
.claude/
.claude.json*
.tmp/
debug/
projects/
shell-snapshots/
statsig/
todos/
*.jsonl

# Runtime artifact: queued work that failed while a provider was down
data/degraded/

# Android signing / API secrets — never commit
android-app/local.properties
local.properties
android-app/app/build/
android-app/.gradle/

# Android binary artifacts — NEVER commit these to git
# They bloat the repo and cause push timeouts
*.apk
*.aab
